using Microsoft.EntityFrameworkCore;
using TESNS.Models;

namespace TESNS.Data
{
    public static class DataSeed
    {
        /*
        public static void SeedCategories(IApplicationBuilder app)
        {
            using var scope = app.ApplicationServices.CreateScope();
            ApplicationDbContext con = scope.ServiceProvider.GetRequiredService<ApplicationDbContext>();

            con.Database.Migrate();
            if (!con.Categories.Any())
                {
                    // Kategorileri oluştur
                    var categories = new[]
                    {
                        new Category { Name = "Technology" },
                        new Category { Name = "Sport" },
                        new Category { Name = "Music" },
                        new Category { Name = "Travel" },
                        new Category { Name = "Literature" }
                        // Diğer kategorileri buraya ekleyebilirsiniz
                    };

                    // Kategorileri veritabanına ekle
                    con.Categories.AddRange(categories);
                    con.SaveChanges();
                }
            }*/
        }
        
        
    }
